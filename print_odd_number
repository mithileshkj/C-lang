#include<stdio.h>
main()
{
    int i=1,count,n;
    printf("Enter a number:");
    scanf("%d",&n);
    for(count=1;count<=n;)
    {
        printf("%d ",i);
        i=i+2;
        count++;
    }
    getch();
}
